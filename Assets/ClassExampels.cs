using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClassExampels : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        List<string> hello = new List<string>(1);
        hello.Add("Wello");
        hello.Add("World!");
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
